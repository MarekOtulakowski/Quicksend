# Copy this file to .env before running `docker compose up` (compose
# loads .env automatically). QUICKSEND_DOMAIN must already have DNS
# pointed at this host so Caddy can obtain a TLS certificate for it
# automatically via Let's Encrypt.
QUICKSEND_DOMAIN=quicksend.example.com
